---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cleanpolicies.cleaner.cloudclub.dev
spec:
  group: cleaner.cloudclub.dev
  names:
    kind: CleanPolicy
    listKind: CleanPolicyList
    plural: cleanpolicies
    singular: cleanpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ttlHours
      name: TTL(H)
      type: integer
    - jsonPath: .spec.schedule
      name: SCHEDULE
      type: string
    - jsonPath: .status.lastCleanupTime
      name: LAST_RUN
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CleanPolicy is the Schema for the cleanpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CleanPolicySpec defines the desired state of CleanPolicy.
            properties:
              jobSelector:
                description: |-
                  JobSelector filters which Jobs are candidates for cleanup.
                  Only Jobs matching this selector will be considered.
                  If nil or empty, all Jobs in the target namespaces are candidates.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              schedule:
                description: |-
                  Schedule for cleanup in duration format (e.g. "1h", "30m").
                  This will be parsed with time.ParseDuration.
                pattern: ^[0-9]+(s|m|h)$
                type: string
              targetNamespaces:
                description: |-
                  TargetNamespaces is an optional list of namespaces to scan.
                  If empty or omitted, the operator will only scan the namespace
                  where this CleanPolicy resource exists.
                items:
                  type: string
                type: array
              ttlHours:
                description: TTL for finished Jobs (in hours).
                format: int32
                minimum: 0
                type: integer
            required:
            - schedule
            type: object
          status:
            description: CleanPolicyStatus defines the observed state of CleanPolicy.
            properties:
              lastCleanupTime:
                description: LastCleanupTime is the last time the cleanup ran.
                format: date-time
                type: string
              lastRunDeleted:
                description: Number of Jobs deleted in the last run.
                format: int32
                type: integer
              totalCleanedJobs:
                description: Total number of Jobs cleaned by this policy.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
